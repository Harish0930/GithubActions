name: Repo Vars and Secrets Example

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Show repository variable
        run: echo "Company is ${{ vars.course }}"

      - name: Use secret key
        env:
          KEY: ${{ secrets.api_key }}
        run: |
          echo "Secret key received"
          echo "$KEY"   # Will be masked in logs