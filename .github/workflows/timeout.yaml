name: Job Timeout Example

on:
  workflow_dispatch:

jobs:
  sample:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Start
        run: echo "Job started"

      - name: Long sleep
        run: sleep 300

      - name: End
        run: echo "Job finished"